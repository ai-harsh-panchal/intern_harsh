<odoo>
    <data>
        <!-- i have inherit product.template model view -->
        <record id="view_product_template_form_inherit_book_tab" model="ir.ui.view">
            <field name="name">product.template.form.inherit.book.tab</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='inventory']" position="after">
                    <page string="Book Details" name="book_details">
                        <group>
                            <group col="2">
                                <field name="is_library_book"/>
                                <field name="author" required="sale_ok"/>
                                <field name="publisher" required="sale_ok"/>
                                <field name="edition" required="sale_ok"/>
                            </group>
                            <group col="2">
                                <field name="published_date" required="sale_ok"/>
                                <field name="pages" required="sale_ok"/>
                                <field name="available" required="sale_ok"/>
                                <field name="status"/>
                            </group>
                        </group>
                    </page>
                </xpath>

                <xpath expr="//page[@name='purchase']" position="inside">
                    <group>
                        <field name="is_vendor"/>
                    </group>
                </xpath>

                <xpath expr="//field[@name='barcode']" position="attributes">
                    <attribute name="string">isbn number</attribute>
                </xpath>
                <xpath expr="//field[@name='published_date']" position="after">
                    <field name="barcode" position="move"/>
                </xpath>
                <xpath expr="//field[@name='standard_price']" position="attributes">
                    <attribute name="readonly">sale_ok</attribute>
                </xpath>
                <xpath expr="//header" position="inside">
                    <button name="action_borrow" type="object" string="Mark as Borrowed"
                            invisible="status in ('return','borrowed')"/>
                    <button name="action_available" type="object" string="Mark as Available"
                            invisible="status != 'borrowed'"/>
                    <button name="action_return" type="object" string="Return Book"
                            invisible="status != 'unavailable'"/>
                    <button name="borrowed_books" type="object" string="Borrowed Books"
                            invisible="status in ('borrowed','unavailable')"/>
                    <field name="status" widget="statusbar" statusbar_visible="available,borrowed,return,unavailable"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
